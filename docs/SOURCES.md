# Sources

The complete versioned release archive contains:

- news endpoints, observation scope, and review status in
  `data/source_registry/unknown.parquet`;
- source-specific publication decisions and evidence in
  `data/rights_matrix/unknown.parquet`;
- frozen release lineage and input hashes in
  `data/build_provenance/unknown.parquet`.

See [RIGHTS.md](RIGHTS.md) for the human-readable source-specific terms. Public
accessibility is not treated as blanket permission to copy publisher text.

